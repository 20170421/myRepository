#include <stdio.h>

int main(void)
{
  // FILE *fp;
  // char *filename = "a.dat";
  // char str[100];
  //
  // if (fp = fopen(filename, "r")) {
  //   while (fgets(str, sizeof(str), fp) != NULL) {
  //     printf("%s", str);
  //   }
  // } else {
  //   fprintf(stderr, "Error occured!\n");
  // }
  //
  // fclose(fp);
  // return 0;

  // char *format = "%#012.5g\n";
  //
  // printf(format, 83.7361);
  // printf(format, 29748.6607);
  // printf(format, 1054932234.0);
  // printf(format, 0.0000235218);

  // int n = -332;
  //
  // printf("%.4d\n", n);
  // printf("%04d\n", n);

  // int num_widget = 1;
  //
  // printf((num_widget == 1)? "%d widget":"%d widgets", num_widget);

  // int i, j, n;
  // float x;
  //
  // n = scanf("%d%f%d", &i, &x, &j);
  //
  // printf("i: %d  x: %f  j: %d  n: %d\n", i, x, j, n);

  return 0;
}
